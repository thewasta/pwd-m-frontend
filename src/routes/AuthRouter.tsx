import React from "react";
import {Switch} from "react-router-dom";

const AuthRouter = () => {
    return (
        <>
            <Switch>
            </Switch>
        </>
    );
};

export default AuthRouter;
